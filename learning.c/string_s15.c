#include<stdio.h>
#include<string.h>
int main()
{
    char str1[10], str2[10];
    printf("Enter the first string:");
    scanf("%s", str1);
    printf("Enter the first string:");
    scanf("%s", str1);
    if((str1, str2)==0)
    {
          printf("string are some");
    }
    else{
        printf("string are not some");

    }
    return 0;



}