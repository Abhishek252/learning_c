#include<stdio.h>

int main()
{

    char arr[] = {'(','{','}',')'};

    if((arr[0]== '(' && arr[3] == ')')&&(arr [1] == '{' && arr[2])
       {

           printf("Matching\n");
       }
       else
        {
            printf("Not matching\n");
        }
        return 0;
}
